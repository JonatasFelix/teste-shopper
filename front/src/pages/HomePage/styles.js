import styled from "styled-components";

export const Container = styled.main`
    margin: 0 auto;
    max-width: 1200px;
    padding: 0 20px;
    display: flex;
    justify-content: space-between;
    margin-top: 30px;
    gap: 10px;
`